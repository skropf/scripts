# get current public IP address
alias myip="curl http://ipecho.net/plain; echo"

# remove all unneeded dependencies on ArchLinux
alias autoremove='sudo pacman -Rsn $(pacman -Qdtq)

# internet speed check
# TOCOME



